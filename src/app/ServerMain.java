package app;

public class ServerMain {
    /**
     * For exporting only, runs the driver
     * @param args
     */
    public static void main(String[] args) {
        app.Server.ServerDriver.main(args);
    }
}
